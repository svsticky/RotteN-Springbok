version: "3.9"

services:
  web:
    build: .
    container_name: RotteN_Springbok
    restart: always
    ports:
      - "${HOST_PORT:-8000}:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./results:/app/results